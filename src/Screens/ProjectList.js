import { getDefaultNormalizer } from '@testing-library/react';
import React from 'react';

const ProjectList = () => {
    return(
        <div>
            <h1>This is the Page where existing list of projects will reside!</h1>
        </div>
    );
};

export default ProjectList;